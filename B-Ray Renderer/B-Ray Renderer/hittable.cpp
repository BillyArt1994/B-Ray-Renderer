#include "hittable.h"

bool Hittable::BoundOctree() {
	//octree_root_ = Octree<Triangle>(mesh_ptr_);
	//octree_root_.OctreeBuild((mesh_ptr_->triangle_list_), bound_, 32u);
	return true;
}

bool Hittable::BulidBounding() {
	return true;
}